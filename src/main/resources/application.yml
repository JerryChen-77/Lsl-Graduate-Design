spring:
  application:
    name: lsl-ai-service-agent
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lsl
    username: root
    password: 082677
  # redis
  data:
    redis:
      host: localhost
      port: 6379
      password:
      ttl: 3600
      database: 0
  profiles:
    active: local
server:
  port: 8123
  servlet:
    context-path: /api
  # springdoc-openapi
  springdoc:
   group-configs:
     - group: 'default'
       packages-to-scan: com.lsl.lslaiseviceagent.controller
# knife4j
  knife4j:
   enable: true
   setting:
    language: zh_cn
